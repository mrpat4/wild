/*eslint-disable */
// import React from 'react';

import titleStrings from "../../string/titleStrings";

const getLocales = () => {
  // let locales = localStorage.getItem(titleStrings.locales);
  // locales = JSON.parse(locales);
  // if (locales) return locales;
  return "";
};

export default getLocales;
