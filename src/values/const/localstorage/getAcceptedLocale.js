/*eslint-disable */
// import React from 'react';

import titleStrings from "../../string/titleStrings";

const getAcceptedLocale = () => {
  // let locales = localStorage.getItem(titleStrings.local);
  // locales = JSON.parse(locales);
  // if (locales) return locales;
  return "";
};

export default getAcceptedLocale;
